{
  "name": "@dicky/examples",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "example": "bun run",
    "test": "bun test",
    "list": "ls src/*.ts | grep -v setup | xargs -n1 basename .ts"
  },
  "dependencies": {
    "@dicky/dicky": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.8"
  }
}
